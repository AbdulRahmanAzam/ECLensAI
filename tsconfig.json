{
  "//": "Root config for the Vercel serverless entrypoint in api/. Each workspace keeps its own tsconfig; this one exists so the function and the apps/api sources it pulls in compile with the same CommonJS + esModuleInterop settings the API is written against.",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "module": "commonjs",
    "moduleResolution": "node",
    "noEmit": true,
    "types": ["node", "multer"]
  },
  "include": ["api/**/*.ts", "apps/api/src/**/*.ts"]
}
